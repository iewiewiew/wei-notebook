#!/bin/bash

#-----------------------------------------------------------------------------
# author: wmh
# 脚本说明：判断上一条命令是否执行成功.
# if [ $? -eq 0 ];then
# $?表示上一条命令返回值，-eq 表示等于
# 如果上一条命令成功执行，返回0，否则返回1（失败）
# [$? -eq 0] 表示如果$?的值等于0，则上一条答命令执行了
#-----------------------------------------------------------------------------

echo "执行成功了吗？"

if [ $? -eq 0 ]; then
    echo "is success!"
else
    echo "is failed!"
fi